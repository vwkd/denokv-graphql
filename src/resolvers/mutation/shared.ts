/**
 * Hacky shared state
 * - get `db` from where??
 *
 * // read `db` from the `context` object where root mutation resolver added it
 * - return promise that resolves with operation result once received all arguments
 */
async function foo() {
}
